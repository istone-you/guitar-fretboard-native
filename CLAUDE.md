# 開発指示：ギター学習アプリ「Guitar Fretboard」

## 1. アプリの概要

「ベースシート（指板の音名または度数）」の上に、「透明なレイヤー（スケール・コード）」を重ね合わせ、ルート音に合わせてスライド（平行移動）させる学習ツール。
Expo + React Native でクロスプラットフォーム（iOS / Android / Web）対応。

## 2. 画面構成と機能要件

### A. ベースレイヤー (Base Sheet)

- **指板構造**: 6弦×15フレット（0〜14フレット）のテーブルレイアウト。
- **データ**: スタンダードチューニング（E2, A2, D3, G3, B3, E4）に基づく全フレットの音名を表示。
- **ベース表示切り替え**: ベースレイヤーは「音名表示」と「度数表示（P1, m2, M2 ...）」をUIで切り替えられること。
  度数表示時は各セルを度数ごとの色枠でも識別できること。
- **音名強調**: 音名表示時、画面下部の「音名」セクションで12音を常時表示し、各音名をクリックして指板上の同名音を強調表示できること。
  - 「絞り込み」ボタン: 現在有効なオーバーレイ（スケール・CAGED・コード）の構成音のみを強調する。
  - 「全強調」/「リセット」ボタン: 強調がない時は「全強調」、強調中は「リセット」（強調なしに戻す）。
- **度数フィルター**: 度数表示時、画面下部の「度数」セクションで各度数をクリックして指板上の表示をオン/オフできること。
  - 「絞り込む」ボタン: 現在有効なオーバーレイ（スケール・CAGED・コード）の構成音のみ表示し、それ以外を非表示にする。
  - 「全削除」/「リセット」ボタン: 全非表示時は「全削除」、非表示がある時は「リセット」（全表示に戻す）。
- **視覚要素**: 3, 5, 7, 9, 12(ダブル)フレットにポジションマークを配置。
- **ルート音ハイライト**: 選択中のルート音は常時ハイライト表示する。オーバーレイがオンの場合はそちらが上書き、オフの場合は薄い赤のリングで表示。

### B. オーバーレイ機能 (Transparent Layers)

以下の各モードを切り替え、または重ねて表示できること。

ボタンは上が背面・下が前面（後半のボタンほど前面に描画される）。

1. **スケールモード**:
   - 1つのモード内で以下をUI切り替えできること。
   - メジャースケール（R, M2, M3, P4, P5, M6, M7）を指板上にハイライト。
   - ナチュラルマイナー（R, M2, m3, P4, P5, m6, m7）を指板上にハイライト。
   - マイナーペンタトニック（R, m3, P4, P5, m7）を指板上にハイライト。
   - メジャーペンタトニック（R, M2, M3, P5, M6）を指板上にハイライト。
   - ブルーノートスケール（R, m3, P4, b5, P5, m7）を指板上にハイライト。
   - ハーモニックマイナー（R, M2, m3, P4, P5, m6, M7）を指板上にハイライト。
   - メロディックマイナー（R, M2, m3, P4, P5, M6, M7）を指板上にハイライト。
   - モード群として Ionian / Dorian / Phrygian / Lydian / Mixolydian / Aeolian / Locrian を指板上にハイライト。
   - スケール選択UIは単純なプルダウンではなく、選択中ラベルを押すと一覧パネルが開く形式とし、候補全体を見渡せること。
2. **CAGEDシステムモード**:
   - C・A・G・E・Dの5つのコードフォームを指板上に表示（複数選択可）。
   - ルート音に応じて各フォームの位置が平行移動すること。
   - フォームは単色（blue）で統一して表示すること。
3. **コードモード**:
   - 1つのモード内で「コードフォーム」「パワーコード」「ダイアトニック」「トライアド」をUIで切り替えられること。
   - コードフォーム表示では、6弦ルートおよび5弦ルートのフォームを同時に表示し、それぞれのフォームを枠で識別できること。
   - オープンコードが存在するコードでは、オープンコードもあわせて表示できること（バレーコードとの視覚的区別は不要）。
   - 対応コード: Major, Minor, 7th, maj7, m7, m7(b5), dim7, m(maj7), sus2, sus4, 6, m6, dim, aug。
   - パワーコード表示では、6弦ルートおよび5弦ルートのフォームを同時に表示し、ルートと5度のみをハイライトすること。
   - ダイアトニック表示では、キー種別（メジャー/マイナー）、和音種別（3和音/4和音）、コードをそれぞれ選べること。対応するダイアトニックコードをコードフォーム表示と同じ要領で表示できること。
   - トライアド表示では、Major / Minor / Diminished / Augmented を選び、弦セット（1~3弦 / 2~4弦 / 3~5弦 / 4~6弦）と転回形（基本 / 第一転回 / 第二転回）をそれぞれ選べること。

### C. クイズ機能

- **クイズモード**: 5種類（note / degree / chord / scale / diatonic）。
- **クイズタイプ**: choice（12択）/ fretboard（指板タップ）/ all（ダイアトニック用）。
- **コード識別**:
  - 指板上にコードフォームを表示し、ルート選択とコード種別選択の2段階で回答。
  - コード種別の出題範囲は複数選択で絞り込めること。
- **スケールクイズ**:
  - クイズパネル内からスケール種類を切り替えられること。
  - 音名選択は構成音を複数選択。
  - 指板は表示範囲内の該当セルをすべて選ぶ。
- **進行**:
  - 回答後は自動遷移せず、明示的なボタンで次へ進むこと。
  - スケールクイズのみ `次へ` ではなく `もう一度` と表示し、同じ問題をリトライ。

## 3. ロジックと操作性

- **ルート選択**: 指板上の任意の音をクリックしてルート音を変更。
- **臨時記号**: ♯/♭ の表記を設定から切り替えられること。
- **トランスポーズ**: ルートを変更すると、コードやスケールの「形」がそのまま平行移動すること。
- **多言語対応**: UI文言は日本語 / 英語を切り替えられること。
  - 実装は `i18next` / `react-i18next` を使用。
  - 翻訳辞書は `src/locales/{en,ja}/common.json` で管理。
- **設定永続化**: 設定は `@react-native-async-storage/async-storage` に保持。
  - ストレージキーは `guiter:` プレフィックス。
  - 対象: テーマ、臨時記号、フレット範囲、表示サイズ、各レイヤー色、言語設定。

## 4. 技術スタック

- **Runtime / Package Manager**: Bun
- **Framework**: Expo (~54.0.0) + React Native (0.81.5)
- **Language**: TypeScript (strict mode)
- **UI**: React 19.1.0
- **Styling**: NativeWind 4 + Tailwind CSS 3
- **SVG**: react-native-svg
- **Bundler**: Metro
- **i18n**: i18next + react-i18next
- **Storage**: @react-native-async-storage/async-storage
- **Path Alias**: `@/*` → `./src/*`（tsconfig.json）
- **Logic**:
  - 音名配列: `['C', 'C#', 'D', 'D#', 'E', 'F', 'F#', 'G', 'G#', 'A', 'A#', 'B']`
  - 度数計算: `(TargetNoteIndex - RootNoteIndex + 12) % 12`

## 5. プロジェクト構成

```
guiter-native/
├── App.tsx                    # ルートコンポーネント（タブナビゲーション）
├── index.js                   # エントリーポイント
├── app.json                   # Expo 設定
├── global.css                 # Tailwind ディレクティブ
├── src/
│   ├── components/
│   │   ├── AppHeader/         # 設定・テーマ・言語・フレット範囲
│   │   ├── FretboardHeader/   # ルート音・ラベルモード切替
│   │   ├── FretboardFooter/   # 音名/度数フィルター
│   │   ├── LayerControls/     # スケール・CAGED・コード・トライアドレイヤー
│   │   ├── NormalFretboard/   # 通常モード指板
│   │   ├── QuizFretboard/     # クイズモード指板
│   │   ├── QuizPanel/         # クイズ出題・回答UI
│   │   └── ui/                # 共通UIコンポーネント
│   │       ├── Fretboard.tsx  # 指板レンダリング（SVG）
│   │       ├── DropdownSelect.tsx
│   │       ├── SegmentedToggle.tsx
│   │       └── scaleOptions.ts
│   ├── hooks/
│   │   ├── useQuiz.ts              # クイズロジック
│   │   ├── useDegreeFilter.ts      # 度数フィルター
│   │   ├── useDiatonicSelection.ts # ダイアトニック選択
│   │   └── usePersistedSetting.ts  # AsyncStorage 永続化
│   ├── logic/
│   │   └── fretboard.ts      # 音楽理論ロジック（スケール・コード・CAGED・度数）
│   ├── types.ts               # 型定義
│   └── locales/
│       ├── en/common.json     # 英語
│       └── ja/common.json     # 日本語
├── tailwind.config.js         # カスタムカラー（fret.*）
├── metro.config.js            # Metro 設定
├── babel.config.js            # Babel 設定（expo preset）
└── tsconfig.json              # TypeScript 設定（strict, パスエイリアス）
```

## 6. 開発ルール

### スクリプト

```bash
bun run start    # Expo 開発サーバー起動
bun run ios      # iOS シミュレーター起動
bun run android  # Android エミュレーター起動
bun run web      # Web 版起動
```

### useEffect の使用について

`useEffect` を使う場合は https://zenn.dev/begineer/articles/8a0696fda04c09 のチェックリストに該当するかどうかを確認すること。該当しない場合は使わないこと。
