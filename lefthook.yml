pre-commit:
  parallel: false
  commands:
    fmt:
      run: mise exec -- bun run fmt:check
    lint:
      run: mise exec -- bun run lint
    typecheck:
      run: mise exec -- bun run typecheck

pre-push:
  commands:
    test:
      run: mise exec -- bun run test
